"use client";

import type { Components } from "streamdown";

export const streamdownComponents: Components = {
  // Customize markdown element rendering here if needed.
  // For now, use defaults — this file exists to satisfy the import.
};
